# Minishell
Developed a Unix-like MiniShell in C for Embedded Linux. Implemented built-in commands, external command execution using fork() and execvp(), command pipelines, job control (jobs, fg, bg), signal handling, custom shell prompt (PS1), and environment variable support using POSIX system calls.
